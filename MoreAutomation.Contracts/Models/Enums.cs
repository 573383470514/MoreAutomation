namespace MoreAutomation.Contracts.Models
{
    public enum RunMode
    {
        Normal,
        WashPet
    }

    public enum AutomationStatus
    {
        Idle,
        Running,
        Paused,
        Error
    }
}