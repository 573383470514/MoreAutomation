<Window x:Class="MoreAutomation.UI.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MoreAutomation - 桌面自动化" Height="650" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="180"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Background="#F3F3F3">
            <Button Content="账号管理" Command="{Binding NavigateToAccountsCommand}" Margin="5"/>
            <Button Content="日常任务" Margin="5"/>
            <Button Content="洗宠模式" Margin="5"/>
        </StackPanel>

        <ContentControl Grid.Column="1" Content="{Binding CurrentView}"/>

        <StackPanel Grid.Column="2" Background="#FAFAFA">
            <TextBlock Text="快捷操作" HorizontalAlignment="Center" Margin="5"/>
            <Button Content="启动脚本" Command="{Binding StartAutomationCommand}" Style="{StaticResource AccentButtonStyle}"/>
            <Button Content="隐藏窗口" Command="{Binding HideWindowCommand}"/>
            <CheckBox Content="自适应开关" Margin="5,10"/>
        </StackPanel>

        <Border Grid.Row="1" Grid.ColumnSpan="3" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <ContentControl Content="{Binding LogConsole}"/>
        </Border>
    </Grid>
</Window>